spring:
  datasource:
    url: jdbc:mysql://localhost:3306/beverage_test?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: root
    hikari:
      maximum-pool-size: 1
  flyway:
    locations: classpath:db/migration
