spring:
  flyway:
    locations: classpath:db/migration,classpath:db/testdata
  datasource:
    url: jdbc:mysql://localhost:3306/beverage?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: root
    hikari:
      maximum-pool-size: 5
      minimum-idle: 3
      idle-timeout: 10000
  jpa:
#    generate-ddl: true
#    hibernate:
#      ddl-auto: update
#      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
#      javax:
#        persistence:
#          schema-generation:
#            scripts:
#              action: create
              #create-target: src/main/resources/ddl.sql #We use this file just once to create the DB.
  jackson:
#    property-naming-strategy: SNAKE_CASE
    deserialization:
      fail-on-unknown-properties: true
      fail-on-ignored-properties: true

  mvc:
    throw-exception-if-no-handler-found: true
    pathmatch:
      matching-strategy: ant-path-matcher

  resources:
    add-mappings: false

  freemarker:
    settings:
      locale: pt_BR

  security:
    oauth2:
      resourceserver:
        jwt:
#          public-key-location: classpath:keys/beverage-pkey.pem
          jwk-set-uri: http://localhost:8080/.well-known/jwks.json # If you want to use a URI
#        opaquetoken:
#          introspection-uri: http://localhost:8081/oauth/check_token
#          client-id: checktoken
#          client-secret: check123

#    user:
#      name: beverageFood
#      password: 123456

#  servlet:
#    multipart:
#      max-file-size: 500KB

  mail:
    host: smtp.gmail.com #e-mail-smtp.us-east-1.amazonaws.com
    port: 587
    username: username
    password: password

beverage:
  storage:
    local:
      photos-directory: /Users/diogo/Documents/Catalog
  jwt:
    keystore:
      jks-location: classpath:keys/beverage.jks
      password: 654321
      keypair-alias: beverage

server:
  compression:
    enabled: true

    s3:
      id-access-key: AcCeSs-KeY # Test Purposes
      key-secret-access: SeCrEt-AcCeSs # Test Purposes
      bucket: beverage-test
      region: us-east-1
      photos-directory: catalog

#    kind: S3
#    kind: local

  email:
    recipient: no-reply@test.com.br
    impl: fake

logging:
  loggly:
    token=xxxxx-xxxx-xxxx-xxxx-xxxxxxxx
#  level:
#    org:
#      springframework: DEBUG